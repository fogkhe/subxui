services:
  subxui:
    build: .
    container_name: subxui
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    env_file:
      - .env
    ports:
      - "31219:8000"
